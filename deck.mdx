import {
  CodeSurfer,
  CodeSurferColumns,
  Step,
} from "code-surfer";
import { Head, github, dracula } from "@code-surfer/themes";

export const theme = dracula;

# Cache Management on Apollo Client v3 
